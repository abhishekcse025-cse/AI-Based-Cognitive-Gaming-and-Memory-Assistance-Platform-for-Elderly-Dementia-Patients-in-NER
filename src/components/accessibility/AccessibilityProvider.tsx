import { AccessibilityProvider } from '../../context/AccessibilityContext';

/**
 * Re-exports AccessibilityProvider for use in the component tree.
 * Kept as its own file to allow future enhancements (e.g., server-side hydration).
 */
export { AccessibilityProvider };
